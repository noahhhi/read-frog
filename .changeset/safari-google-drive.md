---
"@read-frog/extension": patch
---

fix: support Google Drive sign-in in Safari using the existing Google OAuth client and registered extension callback, with login state validation and cancellation handling.
