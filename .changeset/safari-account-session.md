---
"@read-frog/extension": patch
---

fix(safari): read account sessions and authenticated API responses through the official website, preserving streaming and cancellation, and refresh the session after website sign-in
